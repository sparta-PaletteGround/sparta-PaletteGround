import { Posts } from "@/app/_types/detail1/posts";
import React from "react";

const DrawingsList = ({ drawingData }: { drawingData: Posts[] }) => {
  return (
    <section className="w-[1000px] h-[100px] bg-PurplePale rounded-xl">
      DrawingsList
    </section>
  );
};

export default DrawingsList;
